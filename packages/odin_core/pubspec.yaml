name: odin_core
description: Core Odin API client and repository for upload/download flows.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  archive: ^4.0.9
  dio: ^5.9.2
  dotenv: ^4.2.0
  path: ^1.9.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
